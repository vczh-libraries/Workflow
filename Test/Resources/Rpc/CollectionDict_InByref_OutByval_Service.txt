module Rpc;
using system::*;
using RpcWrapperTest::*;
using RpcCollectionDict::InByref::OutByval::*;

func serviceMain(lc : IRpcLifecycle*) : void
{
  var serviceObj = new (IService^)
  {
    var xsService : string[int] = {};

    override func GetServiceResult() : string
    {
      return Print(xsService);
    }

    override func DoList(xs : string[int]) : string[int]
    {
      var xsObj : object = xs;
      if ((xsObj as (system::IRpcWrapperBase^) is null))
      {
        raise "Parameter xs should be a wrapper object in serviceMain";
      }

      xsService = xs;
      xs.Set(4, "D");
      return xs;
    }
  };
  lc.RegisterLocalService(rpc_GetIds()["RpcCollectionDict::InByref::OutByval::IService"], serviceObj);
}
