module Rpc;
using system::*;
using RpcWrapperTest::*;
using RpcCollection::InByref::OutByref::*;

func serviceMain(lc : IRpcLifecycle*) : void
{
  var serviceObj = new (IService^)
  {
    var xsService : int[] = {};

    override func GetServiceResult() : string
    {
      return Print5(xsService);
    }

    override func DoList(xs : int[]) : int[]
    {
      var xsObj : object = xs;
      if ((xsObj as (system::IRpcWrapperBase^) is null))
      {
        raise "Parameter xs should be a wrapper object in serviceMain";
      }

      xsService = xs;
      xs.Add(4);
      return xs;
    }
  };
  lc.RegisterLocalService(rpc_GetIds()["RpcCollection::InByref::OutByref::IService"], serviceObj);
}
