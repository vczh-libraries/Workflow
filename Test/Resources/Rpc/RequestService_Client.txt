module Rpc;
using system::*;

func clientMain(lc : IRpcLifecycle*) : string
{
  var obj = lc.RequestService("RpcTest::IService");
  var service = cast (RpcTest::IService^) obj;
  return service.GetText();
}
