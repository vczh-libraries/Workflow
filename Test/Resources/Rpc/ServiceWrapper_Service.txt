module Rpc;
using system::*;
using RpcServiceWrapperTest::*;

func serviceMain(lc : IRpcLifecycle*) : void
{
  var serviceObj = new (RpcServiceWrapperTest::IService^)
  {
    override func Self(obj : IService^) : bool
    {
      var wrapperObj : object = obj;
      if ((wrapperObj as (system::IRpcWrapperBase^) is not null))
      {
        raise "IService(obj) should be a local object in serviceMain";
      }
      return obj == cast (RpcServiceWrapperTest::IService^) this;
    }
  };
  lc.RegisterLocalService(rpc_GetIds()["RpcServiceWrapperTest::IService"], serviceObj);
}
